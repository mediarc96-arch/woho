# Normalize line endings to LF in the repo so Windows and macOS collaborators
# stay in sync (prevents CRLF/LF churn and noisy diffs).
* text=auto eol=lf

# Binary files should never be normalized
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
